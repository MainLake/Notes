{
  "name": "notes",
  "version": "1.0.0",
  "description": "A simple task management API",
  "main": "src/index.js",
  "scripts": {
    "dev": "pm2 start ecosystem.config.js --only tasks-api-dev && pm2 logs tasks-api-dev",
    "start": "pm2 start ecosystem.config.js --only tasks-api-prod",
    "stop": "pm2 stop all",
    "delete": "pm2 delete all",
    "list": "pm2 list",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "tasks",
    "api",
    "nodejs",
    "express"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "pm2": "^5.4.1"
  }
}